---
description: Key Constraint (KC) page rules
globs:
  - "docs/biological-targets/**/kc/**/*.mdx"
  - "docs/biological-targets/**/kc/**/*.md"
alwaysApply: false
---

Build Gate Proviso:
- Never render or expose spreadsheet letter identifiers in generated content or public-facing pages.
- Always use semantic entity names (for example, `Intervention Dominance`, `Coverage Timing`, `Evidence Type`).
- Treat any letter-identifier wording in generated outputs as a validation failure that must be fixed before build.

When creating or editing Key Constraint (KC) pages:

Follow:
system/key-constraint-schema.md

Structure:
- All sections must be present in the exact order defined in the schema

Field interpretation:
- KC ID + title must be provided
- Overview/description must be provided
- KC relationship context may be provided
- Food/source context may be provided
- Biological role/output context must be provided
- Evidence type + key studies + evidence notes provide evidence context

Rules:
- KCs must be interpreted as substrates/precursors only
- Must not introduce PM->PM chains
- Must not introduce FM->FM dependencies
- Must not include scoring formulas
- Must not include SMs during initial rollout

Food Sources:
- Must include only existing food pages
- If unresolved, flag:
  "Missing system entity: [name]"

References:
- Include only when claims are made
- Use numeric citations [1], [2]
- All references must link to:
  /docs/papers/BRAIN-Diet-References#citationKey
- Verify citationKey exists in static/bibtex/BRAIN-diet.bib
- If missing, flag: "Missing bibliography entry"

If ambiguity exists, stop and ask.
