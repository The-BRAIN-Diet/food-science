# Copy to .env.local (gitignored) and fill in. Never commit real keys.
USDA_API_KEY=

# Local-only Training and QA Registry. Copy this line to .env.local as true.
# Do not commit .env.local.
INCLUDE_INTERNAL_DOCS=
